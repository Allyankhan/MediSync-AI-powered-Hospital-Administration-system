services:
  backend:
    build: ./Backend
    ports:
      - "8000:8000"
    env_file:
      - .env  # Updated to point to the root directory
      
  frontend:
    build: ./Frontend
    ports:
      - "5173:5173"
    depends_on:
      - backend
    volumes:
      - ./Frontend/src:/app/src